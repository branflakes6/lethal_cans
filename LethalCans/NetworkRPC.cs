// TODO
// We will need to implement some networking to calculate our event witnesses
// calculateSpectators()
    // server -> I died xyz
    // server -> every client: player died at xyz
    // clent -> current field of vision
    // server -> calc if u saw
    // server -> send spectator numbers to clients
